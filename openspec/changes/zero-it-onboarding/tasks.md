## 1. Spikes（先做，結果會改變後面的設計）

- [ ] 1.1 確認 LINE Messaging API 接收客戶上傳圖片的流程與大小限制
- [ ] 1.2 決定客戶與工單的綁定方式（出貨時發專屬連結 vs 進來後問識別碼）
- [ ] 1.3 確認 iOS/Android 對 LAN 掃描的實際限制，確定 TCP connect 掃描是否足以回答延後的診斷問題
- [ ] 1.4 驗證能否在無 App 的情況下取得 rebinding protection 偵測結果（例如請客戶用手機瀏覽器開一個連結）；若可行，回報 `deployment-profiles` task 1.4 解除阻塞
- [ ] 1.5 決定未受訓測試者的來源與人數門檻，以及紙本產出流程（Markdown → 印刷檔）

## 2. 修正 README 既有錯誤（可獨立驗證，先做）

- [x] 2.1 `--template onedr0p/cluster-template` 改為 `ferry133/jg-cluster-template`（並改 `--private`：per-user repo 內含加密 secret）
- [x] 2.2 `README.md:205` 的 `just bootstrap talos` 已改為 `task bootstrap:talos`（由 `revive-talos-path` task 7.1 完成——該指令在 ① 移植 `.taskfiles/bootstrap` 的 `talos` 任務後才真正存在）
- [x] 2.3 「6 stages」改為實際的 7
- [x] 2.4 移除 `cp kubeconfig-sa kubeconfig`，改為明確帶 `--kubeconfig`，並說明為何要保留 OIDC 版當救援路徑
- [x] 2.5 全篇掃過；`just bootstrap talos` 已於 ① 修正，其餘指令均對應到實際存在的 task

## 3. 文件拆分

- [x] 3.1 建立 `README.md`（入口）、`README-zero-IT.md`、`docs/deploy/manual.md`
- [x] 3.2 原 README 第 18–354 行原樣搬入 `docs/deploy/manual.md`，逐行 diff 確認無遺漏後才改寫
- [x] 3.3 `README.md` 縮為 78 行（原 354），只做讀者導向與架構說明，**不含任何部署步驟**
- [x] 3.4 `docs/deploy/manual.md` 完整涵蓋 (A) 手動 Talos 與 (B) Omni 兩條路徑，並說明 appliance 為何與手動路徑互斥
- [x] 3.5 operator runbook 僅以連結指向 `factory-agent` change，未複述其步驟
- [x] 3.6 `CLAUDE.md` 新增「文件結構（依讀者拆分）」章節，含紙本同源產生的要求

## 4. 零 IT 客戶文件

- [x] 4.1 撰寫 `README-zero-IT.md`（繁中）：插網路線、接電源、開機三個動作
- [x] 4.2 說明接下來會發生什麼、約 30 分鐘最多一小時、期間不需要做任何事
- [x] 4.3 列出正常但看起來像故障的狀態（風扇聲、燈號閃爍變色、機身溫熱、通知較慢）
- [x] 4.4 失敗時給的是「拍張照傳給我們」這種可執行動作，非錯誤描述；並明寫「不用自己排除問題」與聯絡方式
- [x] 4.5 機器檢查確認：0 個程式碼區塊，無 kubectl/cluster/yaml/task/git 等術語
- [ ] 4.6 建立紙本產出流程，與 `README-zero-IT.md` 同源，並確保改版不會誤印舊版
- [ ] 4.7 設計箱內 QR code（指向溝通管道），並確認它只是輔助入口

## 5. 出貨前準備（消除客戶端步驟）

- [ ] 5.1 確立出貨流程：Talos 預先安裝至內碟，客戶不需選擇開機裝置或進韌體設定
- [ ] 5.2 確立 ISO/schematic 內嵌 SideroLink join token 的流程
- [ ] 5.3 驗證機器插電後自行取得位址並回連管理面，全程無任何路由器或裝置設定

## 6. Onboarding 溝通管道

- [ ] 6.1 在 factory 側部署 onboarding bot（沿用 linebot 的 webhook gateway 形態，獨立部署）
- [ ] 6.2 驗證客戶尚無叢集時管道即可用，且客戶叢集失效時管道仍在
- [ ] 6.3 依 1.2 實作客戶與工單的綁定
- [ ] 6.4 實作三題 intake，答案寫入對應工單
- [ ] 6.5 實作進度推播：工單階段變化 → 非技術語言的訊息
- [ ] 6.6 實作完成通知：系統位址 + 以客戶自己的身分登入
- [ ] 6.7 依 1.1 實作照片請求與接收，並要求請求明確指出要拍什麼
- [ ] 6.8 實作對話式排錯：逐項引導可觀察的現場檢查
- [ ] 6.9 實作升級：引導檢查用盡後轉人工，並告知客戶
- [ ] 6.10 實作客戶回報與照片寫入工單作為證據
- [ ] 6.11 加入防護：送出前檢查訊息不含任何金鑰材料

## 7. 現場診斷

- [ ] 7.1 列舉所有「只有站在客戶網路裡才答得出來」的問題，逐題標明回答機制
- [ ] 7.2 實作以觀察回答的項目（通電、網路線）
- [ ] 7.3 實作以拍照回答的項目（開機狀態）
- [ ] 7.4 把需要探測的項目明確標為延後，並讓它們阻塞時升級人工而非猜測
- [ ] 7.5 依 1.4 實作 rebinding protection 的客戶端偵測與結果記錄
- [ ] 7.6 定義未來 probe 的介面：結果經與客戶觀察相同的證據路徑寫入工單
- [ ] 7.7 驗證加入 probe 不需改動工單模型、流程，也不改變客戶做的三件事

## 8. 驗收

- [ ] 8.1 由 operator 自己走完一次完整 onboarding，修正明顯缺陷
- [ ] 8.2 未受訓讀者實測：只給箱子與紙本，無協助完成三個動作並收到完成通知
- [ ] 8.3 記錄實測中每一次猶豫、發問、做錯，逐項當作文件缺陷修訂
- [ ] 8.4 換一位未受訓讀者重測，至通過為止
- [ ] 8.5 第一台真實客戶交付全程留痕，事後檢討
- [ ] 8.6 回寫所有 spike 結論到 `design.md` 與相關 spec，確認無「待驗證」項目遺留
